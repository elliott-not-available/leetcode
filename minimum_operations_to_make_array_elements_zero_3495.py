# https://leetcode.com/problems/minimum-operations-to-make-array-elements-zero/description/?envType=daily-question&envId=2025-09-06

class Solution:
    def minOperations(self, queries: list[list[int]]) -> int:

        # operation = floor(a  / 4)

        # for each q in query: reduce each q[0] to q[1] to 0 with operation

        # start with brute force, probably not needed

        return
        